package com.algorithmanalysis.secondproject.utils;

/**
 * Error codes 
 *
 * This class is responsible for managing the error codes 
 *
 * @autor Johan Rodriguez
 * @version 1.0
 */
public enum ErrorCodes {
    CROSSOVER_ERROR, // There was an error in the crossover
    ERROR_INCAPABLE, // The program can't generate the desired result
    MAX_ATTEMPTS_EXCEEDED, // The program can't generate the desired result
    NO_ERROR // The program can generate the desired result
}
